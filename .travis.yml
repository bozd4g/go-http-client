language: go

go:
  - 1.x
  - master

install:
  - go get -t -v github.com/google/go-querystring/query
  - go get -t -v github.com/mitchellh/mapstructure

notifications:
  email: true

os: osx

script:
  - go test *_test.go -race -coverprofile=coverage.txt -covermode=atomic ./...

after_success:
  - bash <(curl -s https://codecov.io/bash)